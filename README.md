The data should not have null values.
The column names that were present in the data should be shifted to the header row.
The email and phone number should be in separate columns.
The header rows should contain appropriate column names and not the default names.
The first and last names of the customers should be in separate columns.
There should be a year column and the data should be sorted by products.
